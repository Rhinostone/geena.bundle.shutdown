{
    "home" : {
        "url" : "/",
        "param" : {"action" : "takeOrders"}
    },
    "take-orders" : {
        "requirements" : {
            "bundle" : "([A-Za-z]*)",
            "task" : "([A-Za-z0-9-&+.]*)",
            "pid" : "([0-9]*)",
            "env" : "(?:dev|debug|stage|prod)"
        },
        "url" : "/:bundle/:task/:pid/:env",
        "param" : {"action" : "takeOrders"}
    }
}